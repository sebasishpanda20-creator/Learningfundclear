# Tells GitHub Pages to publish files as-is, without running Jekyll.
# Without this, Jekyll ignores files and folders beginning with an underscore — which
# includes the _headers file that Cloudflare and Netlify need.
